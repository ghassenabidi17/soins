<form method="post" action="{{ path('app_soins_delete', {'idKine': soin.idKine}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ soin.idKine) }}">
    <button class="btn">Delete</button>
</form>
